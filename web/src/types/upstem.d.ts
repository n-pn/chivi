declare namespace CV {
  interface Upstem {
    id: number
    sname: string

    zname: string
    vname: string
    uslug: string

    vintro: string
    labels: string[]

    mtime: number
    guard: number

    chap_count: number
    word_count: number

    viuser_id: number
    wninfo_id: number
  }
}
