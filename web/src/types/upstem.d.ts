declare namespace CV {
  interface Upstem {
    id: number

    sname: string
    mtime: number

    zname: string
    vname: string
    uslug: string

    vintro: string
    labels: string[]

    guard: number
    gifts: number
    multp: number
    wndic: boolean

    chap_count: number
    word_count: number

    viuser_id: number
    wninfo_id: number
  }
}
