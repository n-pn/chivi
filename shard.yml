name: chivi
version: 0.10.0

authors:
  - NP Nam <me@npn.am>

crystal: 1.6.0

license: MIT

targets:
  cvapp-srv:
    main: src/cvapp-srv.cr

  ysapp-srv:
    main: src/ysapp-srv.cr

  wnapp-srv:
    main: src/wnapp-srv.cr

  mt_sp-srv:
    main: src/mt_sp-srv.cr

  mt_v1-srv:
    main: src/mt_v1-srv.cr

  mt_v2-srv:
    main: src/mt_v2-srv.cr

  bcover_cli:
    main: cli/bcover_cli.cr

  ysbook_crawl:
    main: src/ysapp/tasks/ysbook_crawl.cr

  ysuser_crawl:
    main: src/ysapp/tasks/ysuser_crawl.cr

  yslist_crawl:
    main: src/ysapp/tasks/yslist_crawl.cr

  ysrepl_crawl:
    main: src/ysapp/tasks/ysrepl_crawl.cr

  yscrit_crawl_by_user:
    main: src/ysapp/tasks/yscrit_crawl_by_user.cr

  yscrit_crawl_by_list:
    main: src/ysapp/tasks/yscrit_crawl_by_list.cr

  yscrit_crawl_by_book:
    main: src/ysapp/tasks/yscrit_crawl_by_book.cr

  yslist_crawl_by_book:
    main: src/ysapp/tasks/yslist_crawl_by_book.cr

  yslist_crawl_by_user:
    main: src/ysapp/tasks/yslist_crawl_by_user.cr

  seed_yscrits_by_book:
    main: run/yousuu/seed_yscrits_by_book.cr

  seed_yscrits_by_list:
    main: run/yousuu/seed_yscrits_by_list.cr

  seed_yscrits_by_user:
    main: run/yousuu/seed_yscrits_by_user.cr

  fix_yscrits_vhtml:
    main: run/yousuu/fix_yscrits_vhtml.cr

  fix_ysrepls_vhtml:
    main: run/yousuu/fix_ysrepls_vhtml.cr

dependencies:
  tabkv:
    github: chi-vi/tabkv

  crorm:
    github: chi-vi/crorm

  clear:
    github: chi-vi/clear
    branch: master

  lexbor:
    github: kostya/lexbor

  icu:
    github: olbat/icu.cr

  cmark:
    github: amauryt/cr-cmark-gfm

  action-controller:
    github: spider-gazelle/action-controller

  zstd:
    github: didactic-drunk/zstd.cr

  diff:
    github: MakeNowJust/crystal-diff

  email:
    github: arcage/crystal-email

  micrate:
    github: amberframework/micrate

  # zip:
  #   github: n-pn/libzip.cr

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
